{
  "name": "medcut-ios",
  "private": true,
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "format:check": "prettier --check package.json .prettierrc.json .github/workflows/*.yml"
  }
}
